# PHIẾU BÀN GIAO - Quy ước v3.0.0: tài khoản, bảng bản ghi, nhận diện

- **Mã phiếu**: 20260824-06
- **Từ**: agent-phien-06 - **Đến**: phiên sau
- **Thời điểm**: 16:38 24/08/2026
- **Trạng thái**: NỘI DUNG XONG, CHƯA PHÁT HÀNH
- **Nhánh git**: chưa tạo nhánh - thay đổi đang nằm ở cây làm việc trên `main`

## 1. Việc đã làm xong

Năm yêu cầu của chủ project, ghi thành TS-9 tới TS-13, đã xong nội dung.

**TS-9 - `docs/AUTH_AND_ACCOUNT.md`** (mới, 18 mục). `tsudev.com` là Nhà cung cấp
danh tính duy nhất theo OIDC; Google và GitHub nối vào IdP chứ không nối thẳng vào
từng project. Ba lối vào bắt buộc đúng thứ tự. Cơ chế **Xác minh tài khoản** ở mục
8: ân hạn 7 ngày kể từ lúc tạo tài khoản, ba mức hạn chế, huy hiệu có đủ màu-chữ-icon,
nhắc ngày 3 / ngày 6 / lúc hết hạn. Ba hạng sản phẩm A/B/C, trong đó `tsudev-swico`
là hạng C: đăng nhập tùy chọn, `MUST NOT` chặn chức năng lõi, `MUST NOT` gọi mạng ngầm.

**TS-10 - `docs/DATA_TABLE.md`** (mới, 12 mục). Mốc `10 | 20 | 50 | 100 | 200`,
mặc định `10`, **góc dưới bên trái** vùng bản ghi, áp cả cho bảng trong modal và
cả ở màn hình hẹp. Kèm hành vi khi đổi mốc, cách ghi nhớ, hợp đồng với máy chủ.

**TS-11 - `docs/BRAND_ASSETS.md` mục 12 và 13.** Bảng tài sản bắt buộc theo loại
sản phẩm (web / desktop) kèm cổng kiểm CI mẫu, và phần phân biệt kho lưu với kho dùng.

**TS-12 - `AGENTS.md` mục 7.1 + `scripts/check-standards.sh` mục 4.** Quy ước gạch
ngang tách thành mục riêng, phạm vi mở sang tên nhánh, tiêu đề PR, nội dung issue
và câu trả lời của agent. Cổng kiểm mở từ **10 lên 47 đuôi file**, thêm cảnh báo
en-dash, in kèm số dòng vi phạm, sửa tham chiếu sai "mục 6" thành "mục 7.1".

**TS-13 - `docs/ECOSYSTEM_IDENTITY.md`** (mới, 9 mục). Ba vai của `tsudev.com`,
siêu dữ liệu bắt buộc cho web / desktop / kho mã, chuẩn ảnh đại diện dùng chung,
bố cục trang hồ sơ cá nhân.

**Sửa lan theo:** `SEARCH_AND_FILTER.md` mục 7 (trần `page_size` 100 lên 200 kèm
hai ràng buộc bù), `DESIGN_SYSTEM.md` mục 5 (thêm "Vùng bản ghi"; thay 2 en-dash
còn sót), `SECURITY_BASELINE.md` mục 6 (dẫn nhập tách vai ngưỡng / kiến trúc),
`PROJECT_STRUCTURE.md` mục 3.3 (thêm 5 vị trí bắt buộc), `AGENTS.md`,
`docs/00-INDEX.md`, `templates/AGENTS.downstream.md` (đăng ký 3 tài liệu mới),
`VERSION` lên `3.0.0`, `CHANGELOG.md`, `MANIFEST.sha256`.

## 2. Việc dang dở + bước tiếp theo CỤ THỂ

Nội dung xong, **phát hành chưa làm**. Làm theo đúng `docs/VERSIONING.md` mục 1.1:

1. Tạo nhánh `feat/quy-uoc-v3-tai-khoan-bang-nhan-dien`, commit theo nhóm chủ đề
   (đừng gộp một commit khổng lồ - CHANGELOG đã chia sẵn theo nhóm).
2. Mở PR, merge vào `main`.
3. `git tag -a v3.0.0`, push nhãn, tạo GitHub Release, dán mục `3.0.0` của CHANGELOG.
4. **Đọc mục "Hướng dẫn nâng cấp" trước khi đồng bộ.** Bước 1 của nó (dọn em-dash
   ở repo con) `MUST` làm **trước** khi chạy `sync-standards.sh`, nếu không cổng
   kiểm của repo con sẽ đỏ ngay sau khi đồng bộ.
5. Đồng bộ xuống 4 repo con, rồi mở việc `QU-STD-AUTH`, `QU-STD-TABLE`,
   `QU-STD-BRAND` trong `logs/STATE.md` của từng repo. **Chỉ liệt kê, chưa sửa mã**,
   đúng `docs/ONBOARDING.md` mục 2.4.

## 3. File liên quan / đang khóa

| Đường dẫn | Lý do | Còn khóa? |
| --- | --- | --- |
| (không có) | Phiên này đã nhả hết khóa | không |

## 4. Yêu cầu gửi agent đang giữ khóa

Không có.

## 5. Cảnh báo và quyết định quan trọng

**a) Hai mâu thuẫn với quyết định cũ đã được chủ project xử ngay trong phiên.**
Chi tiết ở `logs/STATE.md` mục "Quyết định quan trọng". Tóm tắt:
- `logo-tsudev.png` ở thư mục làm việc **là bản sao đúng từng byte** của
  `assets/brand/variants/tsudev-badge-cyan.png` (md5 `00c334bcfac99073ba534385f8c5637f`).
  Chủ project chọn **giữ nhận diện cam/navy**, hiểu yêu cầu là siết quy tắc bắt
  buộc chứ không đổi ảnh. Nhận diện không đổi ở bản này.
- Trần `page_size` nâng lên 200 **kèm hai ràng buộc bù bắt buộc**. Hai ràng buộc
  đó là điều kiện, không phải khuyến nghị.

**b) Tên file không phải bằng chứng, chuỗi băm mới là bằng chứng.** File tên
`logo-tsudev.png` đặt ngay thư mục làm việc, nghe hoàn toàn như bản chính thức.
Nếu tin cái tên đó thì cả bộ quy ước đã đi ngược lại chính quyết định của chủ
project ghi cách đây vài giờ. Đã ghi thành quy tắc ở `BRAND_ASSETS.md` mục 13.

**c) Bản gốc rời vẫn còn nằm ngoài repo.** `/home/devtsuitstmy/projects/tsudev-standards/logo-tsudev.png`
là bản trùng lặp của file đã được commit trong repo, **chưa xóa** vì nó nằm ngoài
phạm vi repo và cần chủ project quyết. Không có gì mất nếu xóa; nội dung đã an
toàn trong `assets/brand/variants/`.

**d) Cổng kiểm mới đã được kiểm chứng bằng ca âm tính, không chỉ chạy suông.**
Dựng bản sao repo, cắm em-dash vào `.py` và `.html`, en-dash vào `.sql` - cổng
kiểm bắt đúng cả ba, `.sql` ra cảnh báo chứ không chặn. Chạy suông thấy xanh
không chứng minh được cổng kiểm còn hoạt động.

**e) Bản này là MAJOR.** Ba tài liệu mới đều buộc repo con sửa mã. Theo
`docs/VERSIONING.md` mục 1, buộc sửa mã là định nghĩa của thay đổi phá vỡ. Đừng
hạ xuống MINOR cho dễ đồng bộ.

**f) Việc `TS-8` vẫn treo, chưa động tới.** Chữ "dev" hai màu ở `tsudev` và
`tsudev-cwico`. `BRAND_ASSETS.md` mục 12.3 nay ghi rõ đó là **lỗi**, không phải
phong cách, và trỏ về TS-8. Vẫn cần chủ project chọn một trong ba hướng.

## 6. Trạng thái cổng kiểm

- [x] `./scripts/check-standards.sh` đạt - **0 vi phạm, 0 lưu ý**
- [x] `node scripts/build-tokens.mjs --check` đạt
- [x] `node scripts/check-contrast.mjs` đạt
- [x] `bash -n scripts/check-standards.sh` đạt
- [x] Cổng kiểm mục 4 đã kiểm chứng bằng ca âm tính (`.py`, `.html`, `.sql`)
- [x] `MANIFEST.sha256` sinh lại, 49 file, khớp
- [x] Mọi liên kết nội bộ trong `.md` đều trỏ tới file có thật
- [ ] Chưa commit, chưa mở PR, chưa gắn nhãn, chưa đồng bộ repo con

## 7. Kết quả xử lý (agent nhận điền sau khi thực hiện)

- 15:35 25/08/2026 - `agent-phien-07` nhận. Bước 1 của mục 2 **xong**: nhánh
  `feat/quy-uoc-v3-tai-khoan-bang-nhan-dien` đã có 8 commit chia theo nhóm chủ đề
  của CHANGELOG (`3cc6cdf` tới `eeee963`), cổng kiểm xanh 0 lưu ý sau khi commit.
- Bước 2 tới 5 **chưa làm được**: `git push` bị GitHub từ chối `403`. Tài khoản
  `gh` đang hoạt động là `dieuhanhcongviecxanuicam`, không có quyền ghi vào
  `tsudev-tsudev/tsudev-standards`; lệnh `gh auth switch` bị harness chặn.
  Chi tiết và cách gỡ: `logs/handover/20260825-07_phat-hanh-v3-chan-quyen.md`.
